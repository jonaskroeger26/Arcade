Drop production models here to upgrade visuals immediately.

Expected filenames:
- `player.glb` (rigged character, idle + walk/run animations preferred)
- `house.glb` (stylized house module)
- `car.glb` (stylized city car)
- `bus.glb` (stylized city bus)

Notes:
- Models are auto-scaled at runtime and used as replacements for procedural fallback meshes.
- Keep pivots near world origin and base at ground level for best placement.
- Textures should be embedded or referenced relative to this folder.
